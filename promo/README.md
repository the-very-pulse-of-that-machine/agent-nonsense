# Promotional video

Render the 30-second 1080p promotional video, poster, and contact sheet:

```powershell
python .\promo\render_video.py
```

The renderer uses Pillow, NumPy, ImageIO, and ImageIO-FFmpeg. The synthesized audio track and all visuals are generated locally; no external media is required.
